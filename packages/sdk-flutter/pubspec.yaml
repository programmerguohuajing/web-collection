name: web_collection_sdk
description: Official Flutter/Dart SDK for Web Collection APM & Telemetry platform. Provides automatic crash reporting, route tracking, HTTP performance monitoring, and custom event tracking.
version: 0.8.0
homepage: https://github.com/programmerguohuajing/web-collection

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: '^1.1.0'

dev_dependencies:
  test: '^1.24.0'
  lints: '^2.1.0'
